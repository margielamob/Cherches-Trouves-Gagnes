import 'package:flutter_test/flutter_test.dart';

void main() {
  group('reachableGameRequest Tests', () {
    // test('Should create the proper joinableGamesRequest with fromJson', () {
    //   final rawJson = reachableGamesRequestData;
    //   JoinableGamesRequest joinableGamesRequest =
    //       JoinableGamesRequest.fromJson(rawJson);
    //   expect(joinableGamesRequest.games.length, 1);

    //   final firstGame = joinableGamesRequest.games[0];
    //   expect(firstGame.nbDifferences, 5);
    //   expect(firstGame.thumbnail.length, 921654);
    //   expect(firstGame.roomId, "976e90d7-4d8c-44ff-841f-a0cd8ac06a9f");
    // });

    // test('Should create the proper Players with fromJson', () {
    //   final rawJson = reachableGamesRequestData;
    //   JoinableGamesRequest joinableGamesRequest =
    //       JoinableGamesRequest.fromJson(rawJson);

    //   final firstGame = joinableGamesRequest.games[0];
    //   expect(firstGame.players.length, 1);

    //   final player = firstGame.players[0];
    //   expect(player.name, "thierry123");
    //   expect(player.id, "fzSBKn2TVyi25FLPAAAP");
    //   expect(player.avatar, "");
    // });

    // test('Should create the proper gameInformation with fromJson', () {
    //   final rawJson = reachableGamesRequestData;
    //   JoinableGamesRequest joinableGamesRequest =
    //       JoinableGamesRequest.fromJson(rawJson);

    //   final firstGame = joinableGamesRequest.games[0];
    //   expect(firstGame.gameInformation.nbDifferences, 5);
    //   expect(firstGame.gameInformation.thumbnail.length, 921654);
    //   expect(firstGame.gameInformation.idEditedBmp,
    //       "f2384094-ac72-4ffd-84a9-25ee977c41d8");
    //   expect(firstGame.gameInformation.idOriginalBmp,
    //       "bbd9b5c6-f7da-4aa1-ace1-769642286c1c");
    // });
  });
}
